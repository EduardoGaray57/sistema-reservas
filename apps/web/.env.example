# API base URL for the web app.
# Empty value = same-origin requests; the Vite dev server proxies /api to
# localhost:3000. Set the deployed API origin (e.g. https://api.example.com)
# when building for production.
VITE_API_BASE_URL=
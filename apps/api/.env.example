DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5435/sistema_reservas?schema=public"

# Email notifications (Resend)
# Leave RESEND_API_KEY empty to run in degraded mode: booking emails are
# skipped and logged instead of sent. Booking creation is NEVER blocked by
# email failures.
RESEND_API_KEY=""
# Sender shown on booking emails. When unset, defaults to noreply@resend.dev.
EMAIL_FROM=""